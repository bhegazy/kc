steps:
  - label: ":shell: Shellcheck"
    plugins:
      shellcheck#v1.1.1:
        files:
          - kc-init.sh

  - label: ":bash: Test"
    plugins:
      vital-software/docker-compose#vital-v2.5:
        run: test
